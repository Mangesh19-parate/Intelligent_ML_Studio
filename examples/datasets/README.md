# Example Tabular Datasets

Sample tabular datasets for experimentation and verification in Intelligent ML Studio:

- `california_housing.csv`: Standard regression dataset (20,640 samples, 8 features, target: `MedHouseVal`).
- `customer_churn.csv`: Standard binary classification dataset (10,000 samples, 12 features, target: `churn`).
